<% if current_user.nil? || current_user.profile_id != params[:profile_id]%>
  <%= render 'profile/visited_user_profile', locals: {user: @user}%>
<% else %>
  <%= render 'profile/user_profile', locals: {user: @user} %>
<% end %>
  
 
  

  
  
